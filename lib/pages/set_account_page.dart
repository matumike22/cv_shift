import 'package:flutter/material.dart';

class SetAccountPage extends StatelessWidget {
  const SetAccountPage({super.key});

  static const String routeName = '/set_account';
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Set Account Page')));
  }
}
